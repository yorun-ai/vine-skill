interface:
  display_name: "Vine Application Development"
  short_description: "Build Vine services and Portal-delivered vRPC frontends"
  default_prompt: "Use $vine-skill to identify pinned versions and affected Skel boundaries, then build or diagnose the Vine service or generated-TypeScript vRPC frontend; for browser-enabled local Standalone delivery, wire generated Web + WEBGW and RPCGW so the page is usable at port 7288, include native Linux .sh and Windows .bat launchers, and provide actual validation evidence."
